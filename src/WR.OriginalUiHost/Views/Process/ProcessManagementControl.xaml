<UserControl x:Class="WR.OriginalUiHost.ProcessManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppWindowBrush}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Margin="0,0,0,14">
            <TextBlock Text="进程管理"
                       FontSize="22"
                       FontWeight="SemiBold" />
            <DockPanel Margin="0,5,0,0">
                <CheckBox x:Name="AutoAttachCheckBox"
                          DockPanel.Dock="Left"
                          Margin="0,1,16,0"
                          VerticalAlignment="Center"
                          Content="启动时自动接管唯一进程"
                          Checked="AutoAttachCheckBox_OnChanged"
                          Unchecked="AutoAttachCheckBox_OnChanged" />
                <TextBlock x:Name="StatusText"
                           DockPanel.Dock="Left"
                           VerticalAlignment="Center"
                           Text=""
                           Foreground="{DynamicResource AppSubtleTextBrush}"
                           FontSize="13" />
                <Button x:Name="RefreshButton"
                        DockPanel.Dock="Right"
                        Content="刷新"
                        Padding="14,4"
                        Click="OnRefreshClicked" />
            </DockPanel>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  x:Name="ProcessGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                    IsReadOnly="False"
                    RowHeight="38"
                    ColumnHeaderHeight="36"
                    SelectionMode="Single"
                  Background="{DynamicResource AppSurfaceBrush}"
                  Foreground="{DynamicResource AppTextBrush}"
                  BorderThickness="0"
                  RowBackground="{DynamicResource AppSurfaceBrush}"
                  AlternatingRowBackground="{DynamicResource AppSurfaceAltBrush}"
                  HorizontalGridLinesBrush="{DynamicResource AppBorderBrush}"
                  VerticalGridLinesBrush="{DynamicResource AppBorderBrush}">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{DynamicResource AppChromeBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}" />
                    <Setter Property="Padding" Value="10,0" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="10,0" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="接管"
                                        Width="88">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{Binding AttachButtonText}"
                                    Padding="10,2"
                                    MinWidth="58"
                                    Click="OnAttachButtonClicked" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="PID"
                                    Binding="{Binding ProcessId}"
                                    Width="80"
                                    IsReadOnly="True" />
                <DataGridTextColumn Header="窗口"
                                    Binding="{Binding WindowTitle}"
                                    Width="140"
                                    IsReadOnly="True" />
                <DataGridTextColumn Header="角色"
                                    Binding="{Binding CharacterName}"
                                    Width="110"
                                    IsReadOnly="True" />
                <DataGridTextColumn Header="等级"
                                    Binding="{Binding Level}"
                                    Width="56"
                                    IsReadOnly="True" />
                <DataGridTextColumn Header="血量"
                                    Binding="{Binding HealthPercent}"
                                    Width="56"
                                    IsReadOnly="True" />
                <DataGridTextColumn Header="坐标"
                                    Binding="{Binding Position}"
                                    Width="150"
                                    IsReadOnly="True" />
                <DataGridTextColumn Header="接管门"
                                    Binding="{Binding GateState}"
                                    Width="92"
                                    IsReadOnly="True" />
                <DataGridTextColumn Header="状态"
                                    Binding="{Binding State}"
                                    Width="150"
                                    IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
